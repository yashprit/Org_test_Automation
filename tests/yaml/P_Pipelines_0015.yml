pipelines:

  - name: pipelines_P_Pipelines_0015
    configuration:
      chronological: true
    steps:
      - name: P_Pipelines_0015
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
