pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0049
        type: Bash
        configuration:
          type: image
            image: # should be present if type is image
              custom:
                name: drydock/u16nodall
                tag: master
        execution:
          onExecute:
            - node --version
