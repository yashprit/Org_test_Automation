pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0049
        type: Bash
        configuration:
          runtime:
            type: image
            image: # should be present if type is image
              custom:
                name: drydock/u16
                tag: master
        execution:
          onExecute:
            - echo "hi"
