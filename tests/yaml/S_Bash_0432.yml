pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: bash
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"