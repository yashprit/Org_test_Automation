pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          integrations:
            - name: PowerShell_integrations
        execution:
          onExecute:
            - echo "executing step..."
