pipelines:

  - name: pipelines_S_PS_I_DigitalO_0001
    steps:
      - name: S_PS_I_DigitalO_0001
        type: PowerShell
        configuration:
          integrations:
            - name: i_digitalocean
        execution:
          onExecute:
            - echo "executing step..."
