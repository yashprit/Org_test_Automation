pipelines:
  - name: pipeline_automation
    configuration:
      runtime:
        type: host
    steps:
      - name: S_Bash_0068
        type: Bash
        configuration:
          runtime:
            type: image
            image:
              auto:
                language: node
                versions:
                  - 10.18.0
        execution:
          onExecute:
            - |
              [[ "$(node --version)" == *"10.18.0"* ]]
