resources:
  - name: bitbucketServer
    type: GitRepo
    configuration:
      gitProvider: bitbucketServer
      path: ~ranjanks/test
      files:  # optional
        include:
        #exclude: <regularExp>
