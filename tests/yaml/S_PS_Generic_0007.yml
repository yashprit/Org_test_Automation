pipelines:

  - name: pipelines_S_PS_Generic_0007
    steps:
      - name: S_PS_Generic_0007
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - write-output "executing step..."
