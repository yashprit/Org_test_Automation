pipelines:

  - name: pipelines_S_PS_I_GitHubE_0001
    steps:
      - name: S_PS_I_GitHubE_0001
        type: PowerShell
        configuration:
          integrations:
            - name: s_githubEnterprise
        execution:
          onStart:
             - echo "Preparing for work..."
          onExecute:
            - echo "executing step..."
