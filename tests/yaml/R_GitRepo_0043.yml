template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: r_gitlab
    type: GitRepo
    configuration:
      gitProvider: s_gitlab
      path: {{ .Values.GitLab.jfrogtesting.path }}
