pipelines:
  - name: Pipelines_S_Matrix_0014
    steps:
      - name: S_Matrix_0014
        type: Matrix
        stepMode: Bash
        stepletMultipliers:
          environmentVariables:
            - env1: one
            - env2: two
        execution:
          onExecute:
            - printenv
            - echo "Hello world"
