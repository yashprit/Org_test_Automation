pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0013
        type: Bash
        configuration:
          nodePool: u16
          affinityGroup: together
        execution:
          onExecute:
            - echo "hi"

      - name: S_Bash_0013
        type: Bash
        configuration:
          nodePool: u16
          affinityGroup: together
          inputSteps:
            - name: ag_step_1
        execution:
          onExecute:
            - echo "hi"
