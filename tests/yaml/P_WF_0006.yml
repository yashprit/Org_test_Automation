pipelines:

  - name: P_WF_0006
    configuration:
      dependencyMode:
        waitOnParentComplete: "false"
    steps:
      - name: P_WF_0006
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
