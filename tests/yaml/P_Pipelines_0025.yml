pipelines:

  - name: pipelines_P_Pipelines_0025
    configuration:
      dependencyMode:
        waitOnChildComplete: false
    steps:
      - name: P_Pipelines_0025
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
