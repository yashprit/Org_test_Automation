pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0063
        type: Bash
        execution:
          onExecute:
            - echo + onExecute
          onSuccess:
            - echo "onSuccess"
