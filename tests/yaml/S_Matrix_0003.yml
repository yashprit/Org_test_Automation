pipelines:
  - name: Pipelines_S_Matrix_0003
    steps:
      - name: my_Step_1
        type: Matrix
        stepMode: Bash
        configuration:
        execution:
          onExecute:
            - echo "Hello world"
