pipelines:
  - name: pipeline_automation
    steps:
      - name:
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - write-output "executing step..."
