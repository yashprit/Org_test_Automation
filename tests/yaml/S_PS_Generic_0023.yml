pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          timeoutSeconds: 3.7730
        execution:
          onExecute:
            - echo "hi"
