pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          affinityGroup: PowerShellGroup
        execution:
          onExecute:
            - echo "hi"

      - name: api_pipelineauto
        type: PowerShell
        configuration:
          affinityGroup: PowerShellGroup
        execution:
          onExecute:
            - echo "hello"
