pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0050
        type: Bash
        configuration:
          runtime:
            type: image
            image: # should be present if type is image
              custom:
                name: drydock/u16nodall
                tag: master
                autoPull: false
        execution:
          onExecute:
            - echo "hi"
