pipelines:
  - name: pipeline_S_PS_Generic_0053
    steps:
      - name: S_PS_Generic_0053
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
          #  - if($(value) -ne "test1"){
           #   Write-Error "Stop" -ErrorAction Stop
            #  }
           # - Get-Variable
            - $pipeline_name = "updated name which should not happen"
