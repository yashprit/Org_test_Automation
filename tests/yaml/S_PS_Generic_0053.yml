pipelines:
  - name: pipeline_S_PS_Generic_0053
    steps:
      - name: S_PS_Generic_0053
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - $run_number = 'foo'
