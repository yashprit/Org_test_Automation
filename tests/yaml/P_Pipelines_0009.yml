pipelines:
  - name: P_Pipelines_0009
      runtime:       
        type: image
        image:
          auto:
            language: node
            versions:
              - 10.18.0
    steps:
      - name: P_Pipelines_0009
        type: Bash
        execution:
          onExecute:
            - node --version
            - |
              [[ "$(node --version)" == *"10.18.0"* ]]
