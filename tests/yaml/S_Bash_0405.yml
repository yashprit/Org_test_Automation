resources:
  - name: s_bash_0405
    type: RemoteFile
    configuration:
      source: s_artifactory
      fileLocation: auto-generic-local/test.sh
      fileName: test # optional
      fileDateTime: 01/01/2018

pipelines:

  - name: pipelines_auto
    steps:
      - name: S_Bash_0405
        type: Bash
        configuration:
          environmentVariables:
            env1: value1
            env2: value2
          integrations:
            - name: s_artifactory
          inputResources:
            - name: s_bash_0405
        execution:
          onStart:
             - echo "Preparing for work..."
          onExecute:
            - echo "executing step..."
            - jfrog rt dl example-repo-local/test 
