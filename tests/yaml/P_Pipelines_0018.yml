pipelines:

  - name: pipelines_P_Pipelines_0018
    configuration:
      dependencyMode:
        waitOnParentComplete: true
    steps:
      - name: P_Pipelines_0018
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
