pipelines:

  - name: pipelines_P_Pipelines_0064
    configuration:
      retentionPolicy:
        minRuns: 0
    steps:
      - name: P_Pipelines_0064_1
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
