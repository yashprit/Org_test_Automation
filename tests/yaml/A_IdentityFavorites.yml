pipelines:
  - name: Pipelines_IdentityFavorites
    steps:
      - name: api_PipelineSteps
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
            
  - name: Pipelines_IdentityF_admin
    steps:
      - name: api_PipelineSteps
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."  
     
  - name: Pipelines_IdentityF_member
    steps:
      - name: api_PipelineSteps
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."    
          
  - name: Pipelines_IdentityF_projectadmin
    steps:
      - name: api_PipelineSteps
        type: Bash
        execution:
          onExecute:
            - echo "executing step..." 
