pipelines:
  - name: Matrix_pipeline
    steps:
      - name: PreMatrix_step
        type: PreMatrix
        execution:
          onExecute:
            - echo "Executing PreMatrix step...."

      - name: my_Step_1
        type: Matrix
        stepMode: Bash
        configuration:
          inputSteps:
            - name: PreMatrix_step          
        execution:
          onExecute:
            - echo "Hello world"
