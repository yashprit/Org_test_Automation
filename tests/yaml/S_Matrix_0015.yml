pipelines:
  - name: Pipelines_S_Matrix_0015
    steps:
      - name: S_Matrix_0015
        type: Matrix
        stepMode: Bash
        stepletMultipliers:
          environmentVariables:
            - env1: one
            - env2: two
        execution:
          onExecute:
            - |
              if [ ! -z "$env1" ]; then 
                echo "steplet1"
              elif [ ! -z "$env2" ]; then
                echo "steplet2"
              else 
                echo "invalid" 
                exit -1
              fi
