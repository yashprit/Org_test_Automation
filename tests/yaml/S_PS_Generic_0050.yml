pipelines:
  - name: pipelines_S_PS_Generic_0050
    steps:
      - name: S_PS_Generic_0050
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onStart:
            - write-output "onStart"
          onExecute:
            - write-output "onExecute"
          
