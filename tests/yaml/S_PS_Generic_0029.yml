pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          integrations:
            - name: 
        execution:
          onExecute:
            - echo "executing step..."
