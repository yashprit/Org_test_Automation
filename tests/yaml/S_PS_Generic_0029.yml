pipelines:
  - name: pipeline_automation
    steps:
      - name: S_PS_Generic_0029
        type: PowerShell
        configuration:
          nodePool: win_2019
          integrations:
            - name:
        execution:
          onExecute:
            - write-output "executing step..."
