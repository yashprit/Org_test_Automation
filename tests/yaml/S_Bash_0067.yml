pipelines:
  - name: pipeline_automation
    configuration:
      runtime:
        type: image
        image:
          auto:
            language: node
            versions:
              - 10.18.0
    steps:
      - name: S_Bash_0067
        type: Bash
        execution:
          onExecute:
            - |
              [[ "$(node --version)" == *"10.18.0"* ]] 
