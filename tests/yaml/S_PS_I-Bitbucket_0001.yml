pipelines:

  - name: pipelines_S_PS_I-Bitbucket_0001
    steps:
      - name: S_PS_I-Bitbucket_0001
        type: PowerShell
        configuration:
          integrations:
            - name: s_bitbucket
        execution:
          onExecute:
            - echo "executing step..."
