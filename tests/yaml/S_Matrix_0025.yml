pipelines:
  - name: Matrix_pipeline
    steps:
      - name: S_Matrix_0025
        type: Matrix
        configuration:
        execution:
          onExecute:
            - echo "Executing matrix step"

  - name: Matrix_pipeline_Custom
      - name: S_Matrix_0025_Custom
        type: Matrix
        configuration:
        execution:
          onExecute:
            - echo "Executing Matrix step"
