pipelines:
  - name: pipelines_S_Bash_0520
    steps:
      - name: S_Bash_520
        type: Bash
        configuration:
          Integrations:
            - name: bitBucketServerProjectIntegrations
        execution:
          onExecute:
            - echo "executing step..."
