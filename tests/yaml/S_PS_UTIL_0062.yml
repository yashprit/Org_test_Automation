resources:
  - name: repo_pipe
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: jfbetapipeorg/Org_test_Automation

pipelines:
  - name: pipeline_S_PS_UTIL_0062
    steps:
      - name: S_PS_UTIL_0062
        type: PowerShell
        configuration:
          nodePool: win_2019
          # outputResources:
          #   - name: outgoing
          inputResources:
            - name: repo_pipe

        execution:
          onExecute:
            - get-childitem $run_dir\workspace
            - add_pipeline_variables pipelineVariable="asdffdjksfj.       k455457457"



      - name: S_PS_UTIL_0062_2
        type: PowerShell
        configuration:
          nodePool: win_2019
          inputResources:
            - name: repo_pipe
          inputSteps:
            - name:  S_PS_UTIL_0062

        execution:
          onExecute:
            - if($res_repo_pipe_pipelineVariable -eq $null){ Write-Error "Stop" -ErrorAction Stop}         

