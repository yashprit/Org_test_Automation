resources:
  - name: S_PS_R_IMAGE_0015_Image
    type: Image
    configuration:
      registry: i_aws
      imageName: ubuntu
      imageTag: latest
      region: us-east-1

pipelines:
  - name: pipelines_S_PS_R_IMAGE_0015
    steps:
      - name: S_PS_R_IMAGE_0015
        type: PowerShell
        configuration:
          inputResources:
            - name: S_PS_R_IMAGE_0015_Image
              trigger: true
        execution:
          onExecute:
            - echo "executing step..."
