pipelines:
  - name: pipelines_S_PS_Generic_0052
    steps:
      - name: S_PS_Generic_0052
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - write-output "onExecute"
          onComplete:
            - write-output "onComplete"
