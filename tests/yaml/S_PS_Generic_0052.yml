pipelines:
  - name: pipelines_S_PS_Generic_0052
    steps:
      - name: S_PS_Generic_0052
        type: PowerShell
        execution:
          onExecute:
            - echo "onExecute"
          onComplete:
            - echo "onComplete"
