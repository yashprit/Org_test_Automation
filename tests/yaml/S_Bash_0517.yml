pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0517
        type: Bash
        configuration:
          runtime:
            type: image
            image:
              auto:
                language: cpp
                versions:
                  - "7.0.1"
        execution:
          onExecute:
            - clang --version
            - gcc --version
            - |
              [[ "$(clang --version)" == *"7.0.1"* ]]
