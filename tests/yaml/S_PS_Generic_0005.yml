pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"
