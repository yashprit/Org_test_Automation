pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0042
        type: Bash
        configuration:
          runtime:
            type: image
            image:
              auto:
                language: java
                versions:
                  - "11"
        execution:
          onExecute:
            - |
              [[ "$step_image_name:$step_image_tag" == "docker.bintray.io/jfrog/pipelines-u18java:11" ]]
            - |
              [[ "$(java --version)" == *"$step_image_tag"* ]]
