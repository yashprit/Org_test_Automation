resources:
  - name: win_customImage
    type: Image
    configuration:
      registry: varsha_docker
      imageName: shippabledocker/jfreq_win
      imageTag: latest

pipelines:
  - name: pipeline_S_PS_UTIL_0026
    steps:
      - name: S_PS_UTIL_0026
        type: PowerShell
        configuration:
          nodePool: win_2019
          outputResources:
            - name: repo_pipe
        execution:
          onExecute:
            - write_output win_customImage "imageTag=masterarm"
