resources:
  # This resource is for BitBucketServer
  - name: r_GitRepo
    type: GitRepo
    configuration:
      path: PIP/automation
      branches:
        include: master
      gitProvider: s_artifactory
