pipelines:

  - name: P_WF_0005
    configuration:
      dependencyMode:
        waitOnParentComplete: false
    steps:
      - name: P_WF_0005
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
