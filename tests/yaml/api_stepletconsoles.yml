pipelines:
  - name: api_stepletconsoles
    steps:
      - name: s_stepletconsoles
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "hi"
