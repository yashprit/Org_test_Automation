pipelines:

  - name: pipelines_P_Pipelines_0026
    configuration:
      dependencyMode:
        waitOnChildComplete: "false"
    steps:
      - name: P_Pipelines_0026
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
