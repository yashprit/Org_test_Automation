resources:
  # This resource is for BitBucketServer
  - name: r_GitRepo
    type: Git
    configuration:
      path: PIP/automation
      branches:
        include: master
      gitProvider: s_bitbucketServer
