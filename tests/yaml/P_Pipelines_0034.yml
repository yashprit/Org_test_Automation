pipelines:

  - name: pipelines_P_Pipelines_0034_1
    steps:
      - name: P_Pipelines_0034_1
        type: Bash
        configuration:
          outputResources:
            - name: P_Pipelines_0034_resource
        execution:
          onExecute:
            - echo "executing step..."
            - sleep 10
            - write_output P_Pipelines_0034_resource test=$run_number

  - name: pipelines_P_Pipelines_0034_2
    configuration:
      dependencyMode:
        waitOnParentSuccess: true
        waitOnParentComplete: true
    steps:
      - name: P_Pipelines_0034_2
        type: Bash
        configuration:
          inputResources:
            - name: P_Pipelines_0034_resource
        execution:
          onExecute:
            - echo "executing step..."
            - sleep 10


resources:
  - name: P_Pipelines_0034_resource
    type: PropertyBag
    configuration:
      test: test
