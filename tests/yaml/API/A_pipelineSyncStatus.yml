pipelines:
  - name: pipelineSyncStatus_automation
    steps:
      - name: api_pipelineSyncStatus
        type: Bash
        configuration:
          integrations:
            - name: s_gitHub
        execution:
          onExecute:
            - echo "hi"
