pipelines:

  - name: pipelines_P_Pipelines_0020
    configuration:
      dependencyMode:
        waitOnParentComplete: "false"
    steps:
      - name: P_Pipelines_0020
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
