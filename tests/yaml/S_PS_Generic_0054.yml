pipelines:
  - name: pipeline_S_PS_Generic_0054
    configuration:
      runtime:
        type: image
        image:
          auto:
            language: node
            versions:
              - 12.14.0
    steps:
      - name: pipeline_S_PS_Generic_0054
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
            - node --version
            - |
              [[ "$(node --version)" == *"12.14.0"* ]]
