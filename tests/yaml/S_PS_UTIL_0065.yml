resources:
  - name: S_PS_UTIL
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: jfbetapipeorg/Org_test_Automation

pipelines:
  - name: pipeline_S_PS_UTIL_0065
    steps:
      - name: S_PS_UTIL_0065
        type: PowerShell
        configuration:
          nodePool: win_2019
          inputResources:
            - name: S_PS_UTIL

        execution:
          onExecute:
            - Push-Location $res_S_PS_UTIL_resourcePath
            - get-childitem $run_dir\workspace
            - get-childitem $res_S_PS_UTIL_resourcePath
            - add_pipeline_files -sources "$res_S_PS_UTIL_resourcePath\tests\yaml\test_sample.py" -name newFile


      - name: S_PS_UTIL_0065_2
        type: Bash
        configuration:
          inputResources:
            - name: S_PS_UTIL
          inputSteps:
            - name:  S_PS_UTIL_0065

        execution:
          onExecute:
            - restore_pipeline_files newFile test_sample.py
            - ls
            - cat test_sample.py
