pipelines:
  - name: PostMatrix_pipeline
    steps:
      - name: PreMatrix_step
        type: PreMatrix
        execution:
          onExecute:
            - echo "Hello World"
            - echo "Fan out working" > PreMatrix_1.txt
            
      - name: PreMatrix_step2
        type: PreMatrix
        execution:
          onExecute:
            - echo "Hello World"
            - echo "Fan out working" > PreMatrix_1.txt     

      - name: PostMatrix_step
        type: PostMatrix
        configuration:
          inputSteps: 
            - name: PreMatrix_step
            - name: PreMatrix_step2
        execution:
          onExecute:
            - echo "Executing fanin step"
            - printenv
            
            - ls -l ./matrix_step1/PreMatrix_step
            - tem=$( ls -l ./matrix_step1/PreMatrix_step | sed -n '2p' )
            - echo $tem
            - x=$( echo $tem | awk -F " " '{print $NF}' )
            - echo $x
            - |
              [[ "$x" == "PreMatrix_1.txt" ]]
