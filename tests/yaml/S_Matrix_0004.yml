pipelines:
  - name: Pipelines_S_Matrix_0004
    steps:
      - name: my_Step_1
        type: Matrix
        stepMode: Bash
        configuration:
        stepletMultipliers:
          environmentVariables:
            - env1: one
        execution:
          onExecute:
            - echo "Hello world"
