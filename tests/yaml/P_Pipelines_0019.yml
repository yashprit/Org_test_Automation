pipelines:

  - name: pipelines_P_Pipelines_0019
    configuration:
      dependencyMode:
        waitOnParentComplete: false
    steps:
      - name: P_Pipelines_0019
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
