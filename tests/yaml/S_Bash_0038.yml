pipelines:
  - name: pipeline_automation
    configuration:
      runtime:
        type: image
        image:
          custom:
            name: drydock/u16javall
            tag: master

    steps:
      - name: S_Bash_0038
        type: Bash
        configuration:
          runtime:
            type: host
            image:
              custom:
                name: drydock/u16nodall
                tag: master
        execution:
          onExecute:
            - echo "executing step..."
            - |
              [ "$step_image_name" == "drydock/u16nodall"  ]
