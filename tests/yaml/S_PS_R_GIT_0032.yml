
resources:
  - name: S_PS_R_GIT_0032_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: jfbetapipeorg/Org_test_Automation

pipelines:
  - name: pipelines_S_PS_R_GIT_0032
    steps:
      - name: S_PS_R_GIT_0032
        type: PoerShell
        configuration:
          inputResources:
            - name: S_PS_R_GIT_0032_GitRepo
        execution:
          onExecute:
            - echo "executing step..."
