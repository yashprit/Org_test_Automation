pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          Runtime:
        execution:
          onExecute:
            - echo "executing step..."
