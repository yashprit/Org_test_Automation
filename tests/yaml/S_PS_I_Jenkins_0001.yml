pipelines:
  - name: pipelines_S_PS_I_Jenkins_0001
    steps:
      - name: S_PS_I_Jenkins_0001
        type: PowerShell
        configuration:
          integrations:
            - name: i_jenkins
        execution:
          onExecute:
            - echo "executing step..."
