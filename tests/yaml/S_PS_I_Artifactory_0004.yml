pipelines:
  - name: pipelines_S_PS_I_Artifactory_0004
    steps:
      - name: S_PS_I_Artifactory_0004
        type: PowerShell
        configuration:
          integrations:
            - name: s_artifactory
        execution:
          onExecute:
            - echo "executing step..."
