pipelines:
  - name: P_Pipelines_0073
    configuration:
      environmentVariables:
        readOnly:
          pipe_env_var:
            default: value1
            values: value1,value2
    steps:
      - name: P_Pipelines_0073_1
        type: Bash
        execution:
          onExecute:
            - echo $pipe_env_var
