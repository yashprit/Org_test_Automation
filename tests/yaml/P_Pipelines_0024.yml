pipelines:

  - name: pipelines_P_Pipelines_0024
    configuration:
      dependencyMode:
        waitOnChildComplete: true
    steps:
      - name: P_Pipelines_0024
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
