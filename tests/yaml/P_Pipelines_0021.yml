pipelines:

  - name: pipelines_P_Pipelines_0021
    configuration:
      dependencyMode:
        waitOnParentSuccess: true
    steps:
      - name: P_Pipelines_0021
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
