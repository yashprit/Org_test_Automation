pipelines:

  - name: pipelines_S_PS_I_BitbucketS_0003
    steps:
      - name: S_PS_I_BitbucketS_0003
        type: PowerShell
        configuration:
          integrations:
            - name: s_bitbucketServer
        execution:
          onExecute:
            - echo "executing step..."
