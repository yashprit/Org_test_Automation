pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0428
        type: Bash
        configuration:
          integrations: 
            - name: i_DockerRegistry
          runtime:   
            type: image
            image: 
              custom:
                name: shipbeta00/ubuntu
                tag: master
                autoPull: true
        execution:
          onExecute:
            - printenv
            - echo "hi"
