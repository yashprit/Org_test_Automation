pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0428
        type: Bash
        configuration:
          integrations: 
            - name: docker # i_DockerRegistry
          runtime:   
            type: image
            image: 
              custom:
                name: shippabledocker/sample_node_pvt #shipbeta00/ubuntu #app
                tag: masterpyt # master  #latest
                autoPull: true
        execution:
          onExecute:
            - printenv
            - echo "hi"
