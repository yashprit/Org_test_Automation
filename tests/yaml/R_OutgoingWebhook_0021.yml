resources:
  - name: outgoingWebhook
    type: OutgoingWebhook
    configuration:
      webhookName: outgoingResource3
      #method: GET #default POST
      #Parameters: <query string to be added>
     # path: “/api/v1/issues”
      #headers:
       # accept: xyc
