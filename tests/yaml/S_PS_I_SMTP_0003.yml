pipelines:
  - name: pipelines_S_PS_I_SMTP_0003
    steps:
      - name: S_PS_I_SMTP_0003
        type: PowerShell
        configuration:
          integrations:
            - name: i_smtp
        execution:
          onExecute:
            - echo "executing step..."
