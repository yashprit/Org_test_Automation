pipelines:
  - name: pipeline_S_PS_Generic_0035
    steps:
      - name: S_PS_Generic_0035
        type: PowerShell
        configuration:
          nodePool: win_2019
          runtime:
            type: host
        execution:
          onExecute:
            - if($step_runtime -ne "host"){
              Write-Error "Stop" -ErrorAction Stop
              }
           
