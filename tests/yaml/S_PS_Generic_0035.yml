pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: PowerShell
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
            - |
              [ $step_runtime == "host" ]
