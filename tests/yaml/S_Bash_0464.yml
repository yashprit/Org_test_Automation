pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0464
        type: Bash
        configuration:
          runtime:
            type: image
            image:
              auto:
                language: java
                versions:
                  - "8"
        execution:
          onExecute:
            - |
              [[ "$(java --version)" == *"$step_image_tag"* ]]
