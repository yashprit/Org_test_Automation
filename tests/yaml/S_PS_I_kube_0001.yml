pipelines:
  - name: pipelines_S_PS_I_kube_0001
    steps:
      - name: S_PS_I_kube_0001
        type: PowerShell
        configuration:
          integrations:
            - name: i_kubernetes
        execution:
          onExecute:
            - echo "executing step..."
