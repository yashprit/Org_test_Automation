pipelines:
  - name: Pipelines_S_Matrix_0017
    steps:
      - name: S_Matrix_0017
        type: Matrix
        stepMode: Bash
        configuration:
          environmentVariables: 
            flag: none
        stepletMultipliers:
          runtimes:
            - type: image
              image:
                auto:
                  language: node
                  versions:
                    - 10.21.0
            - type: image
              image:
                auto:
                  language: node
                  versions:
                    - 8.17.0
        execution:
          onExecute:
            - printenv
            - echo "Hello world"
            - set_step_var test $step_name
            - testValue=`get_step_var S_Matrix_0017 test`
            - |
              if [ ! -z "$env1" ]; then
                if [ "$testValue" == "S_Matrix_0017" ]; then
                  echo "success"
                else
                  exit -1
                fi
              fi
            - |
              if [ ! -z "$env2" ]; then
                if [ "$testValue" == "S_Matrix_0017" ]; then
                  echo "success"
                else
                  exit -1
                fi
              fi


            
