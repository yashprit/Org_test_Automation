pipelines:
  - name: pipeline_S_PS_Generic_0009
    steps:
      - name: S_PS_Generic_0009
        type: PowerShell
        execution:
          onExecute:
            - echo "executing step..."
            - |
              [ $step_node_pool_name == "automation_node_default"  ]
