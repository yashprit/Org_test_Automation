pipelines:
  - name: pipelines_S_PS_I_Jira_0001
    steps:
      - name: S_PS_I_Jira_0001
        type: PowerShell
        configuration:
          integrations:
            - name: i_jira
        execution:
          onExecute:
            - echo "executing step..."
