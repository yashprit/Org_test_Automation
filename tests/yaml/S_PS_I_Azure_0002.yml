pipelines:

  - name: pipelines_S_PS_I_Azure_0002
    steps:
      - name: S_PS_I_Azure_0002
        type: PowerShell
        configuration:
          integrations:
            - name: i_azureKey
        execution:
          onExecute:
            - echo "executing step..."
