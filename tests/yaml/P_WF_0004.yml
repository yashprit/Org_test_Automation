pipelines:

  - name: P_WF_0004
    configuration:
      dependencyMode:
        waitOnParentComplete: true
    steps:
      - name: P_WF_0004
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."
