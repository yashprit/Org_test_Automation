
resources:
  - name: S_PS_R_GIT_0014_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitlab
      path: jfbeta/test_automation
      branches:
        include: master
      buildOn:  # optional
        tagCreate: true

pipelines:
  - name: pipelines_S_PS_R_GIT_0014
    steps:
      - name: S_PS_R_GIT_0014
        type: PowerShell
        configuration:
          inputResources:
            - name: S_PS_R_GIT_0014_GitRepo
              trigger: false            
        execution:
          onExecute:
            - echo "executing step..."
