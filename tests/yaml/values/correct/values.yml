GitRepo:
  name: fooGitRepo
  gitProvider: s_github_template
  path: Bharath92/Org_test_Automation
  branches:
    include: 'master'

artifactoryIntegration: s_artifactory_template
  
Image:
  name: fooImage
  sourceRepository: foo

DockerBuild:
  name: foobuild
  dockerImageName: foo
  dockerFileName: foo
  dockerFileLocation: '.'
  dockerImageTag: ${run_number}

Pipeline:
  name: foo

DockerPush:
  name: foopush
  dockerImageName: foo
  dockerFileName: foo
  targetRepository: foo
